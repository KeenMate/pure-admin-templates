{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://keenmate.com/schemas/pure-admin-template.schema.json",
  "title": "Pure Admin Template Manifest",
  "description": "Configuration manifest for Pure Admin project templates",
  "type": "object",
  "required": ["id", "name", "version", "technology", "variant"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Reference to the JSON schema for validation"
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Unique identifier for the template (lowercase, hyphens allowed). Matches the folder name."
    },
    "name": {
      "type": "string",
      "description": "Human-readable display name (e.g. 'SvelteKit + Pure Admin')"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+(-[a-zA-Z0-9.]+)?$",
      "description": "Semantic version of the template"
    },
    "technology": {
      "type": "string",
      "description": "Technology group for wizard selection (e.g. svelte, react, elixir)"
    },
    "variant": {
      "type": "string",
      "description": "Variant within the technology (e.g. sveltekit, spa, vite, nextjs)"
    },
    "description": {
      "type": "string",
      "description": "Brief one-line description of the template"
    },
    "content": {
      "type": "string",
      "description": "Markdown content for the template detail page on pureadmin.io"
    },
    "author": {
      "type": "string",
      "description": "Template author or organization"
    },
    "license": {
      "type": "string",
      "description": "License identifier (e.g. MIT, Apache-2.0)"
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Searchable tags (e.g. 'ssr', 'spa', 'static', 'fullstack')"
    },
    "homepage": {
      "type": "string",
      "format": "uri",
      "description": "URL to the template's homepage or repository"
    },
    "scaffold": {
      "type": "object",
      "description": "Commands to bootstrap the project before template files are applied",
      "properties": {
        "command": {
          "type": "string",
          "description": "Primary scaffold command. Supports __APP_ID__ and __SCAFFOLD_FLAGS__ placeholders. __SCAFFOLD_FLAGS__ is built from feature.scaffoldFlag entries (see features schema)."
        },
        "fallback": {
          "type": "string",
          "description": "Fallback command if primary fails"
        },
        "runFirst": {
          "type": "boolean",
          "default": false,
          "description": "When true and --template-path is used, runs the scaffold command BEFORE copying template files. Lets templates run a generator (e.g. mix phx.new) and then layer customizations on top of the result."
        }
      }
    },
    "dependencies": {
      "type": "object",
      "description": "npm packages to install after scaffold (key: package name, value: version)",
      "additionalProperties": { "type": "string" }
    },
    "placeholders": {
      "type": "object",
      "description": "Template variables substituted at create time (__VAR__ syntax)",
      "additionalProperties": {
        "type": "object",
        "required": ["description"],
        "properties": {
          "description": {
            "type": "string",
            "description": "What this placeholder is for"
          },
          "default": {
            "type": "string",
            "description": "Default value if not provided"
          }
        }
      }
    },
    "features": {
      "type": "object",
      "description": "Template features that can be toggled via CLI flags or the wizard",
      "additionalProperties": {
        "type": "object",
        "required": ["description", "points"],
        "properties": {
          "description": {
            "type": "string",
            "description": "What this feature provides"
          },
          "isRequired": {
            "type": "boolean",
            "default": false,
            "description": "If true, the feature cannot be disabled (always included)"
          },
          "isDefault": {
            "type": "boolean",
            "default": false,
            "description": "If true, the feature is enabled by default (user can disable)"
          },
          "requires": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Feature IDs that must be enabled when this feature is enabled"
          },
          "points": {
            "type": "array",
            "items": { "type": "string" },
            "description": "data-pa point IDs that belong to this feature"
          },
          "scaffoldFlag": {
            "type": "string",
            "description": "Flag to add to the scaffold command's __SCAFFOLD_FLAGS__ when this feature is DISABLED. Example: '--no-ecto' for the ecto feature in a Phoenix template. Alias: scaffoldFlagWhenDisabled."
          },
          "scaffoldFlagWhenDisabled": {
            "type": "string",
            "description": "Same as scaffoldFlag — flag added to __SCAFFOLD_FLAGS__ when feature is disabled."
          },
          "scaffoldFlagWhenEnabled": {
            "type": "string",
            "description": "Flag to add to __SCAFFOLD_FLAGS__ when this feature is ENABLED. Example: '--database postgres' when a postgres feature is on."
          },
          "cli": {
            "description": "CLI flag(s) to enable this feature. String for single flag, array of objects for multiple exclusive options.",
            "oneOf": [
              {
                "type": "string",
                "description": "Single CLI flag (e.g. '--profile-panel')"
              },
              {
                "type": "array",
                "description": "Multiple exclusive options for this feature (pick one)",
                "items": {
                  "type": "object",
                  "required": ["name", "flag"],
                  "properties": {
                    "name": {
                      "type": "string",
                      "description": "Display name for this option"
                    },
                    "flag": {
                      "type": "string",
                      "description": "CLI flag (e.g. '--font-awesome')"
                    },
                    "isDefault": {
                      "type": "boolean",
                      "default": false,
                      "description": "Whether this option is the default choice"
                    }
                  }
                }
              }
            ]
          }
        }
      }
    },
    "pageTypes": {
      "type": "object",
      "description": "Available page generators for --preset and --pages",
      "additionalProperties": {
        "type": "object",
        "required": ["file", "description"],
        "properties": {
          "file": {
            "type": "string",
            "description": "Path to page template file (relative to template root)"
          },
          "description": {
            "type": "string",
            "description": "What kind of page this generates"
          },
          "route": {
            "type": "string",
            "description": "Override route path pattern (default: src/routes/{{entity}}/+page.svelte)"
          },
          "icon": {
            "type": "string",
            "description": "Default sidebar icon class"
          },
          "defaultLabel": {
            "type": "string",
            "description": "Default sidebar label"
          },
          "sidebar": {
            "type": "boolean",
            "default": true,
            "description": "Whether this page type should appear in the sidebar. Set to false for parameterized routes like detail views that aren't reachable from a top-level link."
          }
        }
      }
    },
    "instructions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Post-create instructions shown to the user (supports placeholders)"
    },
    "checksums": {
      "type": "object",
      "description": "SHA-256 checksums of template files for change detection on upload",
      "properties": {
        "files": {
          "type": "object",
          "description": "SHA-256 checksums of all files in template/, keyed by relative path",
          "additionalProperties": {
            "type": "string",
            "pattern": "^sha256:[a-f0-9]{64}$"
          }
        },
        "pages": {
          "type": "object",
          "description": "SHA-256 checksums of page generator files, keyed by relative path",
          "additionalProperties": {
            "type": "string",
            "pattern": "^sha256:[a-f0-9]{64}$"
          }
        },
        "metadata": {
          "type": "string",
          "pattern": "^sha256:[a-f0-9]{64}$",
          "description": "SHA-256 of canonical JSON encoding of descriptive metadata (name, version, description, content, author, license, tags)"
        },
        "summary": {
          "type": "string",
          "pattern": "^sha256:[a-f0-9]{64}$",
          "description": "SHA-256 over all individual checksums (files, pages, helper, metadata) — single value to detect any change"
        }
      }
    }
  }
}
